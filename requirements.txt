# Core dependencies for FLUX image generation
# NOTE: IP-Adapter support for FLUX requires a newer diffusers version.
diffusers>=0.35.0,<0.40.0  # Compatible with PyTorch 2.4.x
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.5
pillow>=10.4.0
huggingface_hub>=0.24.6
python-dotenv>=1.0.1

# PyTorch with CUDA support for GPU acceleration
# IMPORTANT: Choose the wheel that matches your CUDA version on Runpod
# For CUDA 12.4: torch>=2.4.0 --index-url https://download.pytorch.org/whl/cu124
# For CUDA 12.1: torch>=2.1.0 --index-url https://download.pytorch.org/whl/cu121
# For CUDA 11.8: torch>=2.1.0 --index-url https://download.pytorch.org/whl/cu118
torch>=2.4.0
torchvision>=0.19.0

# Additional dependencies that may be needed
numpy>=1.24.0
requests>=2.31.0
protobuf>=3.20.0  # Required for sentencepiece tokenizers used by FLUX
sentencepiece>=0.1.99  # Required for T5 tokenizers used in FLUX

# PEFT for LoRA support (required for LoRA functionality)
peft>=0.7.0
